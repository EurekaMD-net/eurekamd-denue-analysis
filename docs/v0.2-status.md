# v0.2.x Roadmap Status

> Read this doc on resume. It survives `/compact` and tells the next session
> exactly what's loaded, what's blocked, and what's queued. Companion to
> `docs/analyzer-plan-v1.md` (frontend) — this one is the data-integration
> side.

## Where we are (2026-05-04)

```
v0.1     ✅ DENUE base — extraction, schema, loader, pipeline, analysis, HTTP API
v0.2.1   ✅ Censo 2020 + CONEVAL Pobreza + CONEVAL IRS — municipal-level joins live
         ⏳ AGEB-level disaggregates — gated portal, operator-assist required
v0.2.2   🟡 CLUES — DGIS catálogo de salud, 41,381 unidades EN OPERACION (loaded 2026-05-04)
         ⏳ CE 2024 — gated SAIC portal, operator-assist needed for ZIP URLs
         ⏳ SESNSP — gob.mx incidencia delictiva slug stale, URL discovery pending
v0.2.3   📋 Datatur + SINAIS + ENOE + ENIGH — after v0.2.2
v0.3 P2  ✅ Locust mode — 5 ECharts + 4 /analytics endpoints (shipped 2026-05-04)
v0.3 P3  ✅ Map mode — MapLibre + Carto basemap + MVT vector source + deck.gl cluster overlay (shipped 2026-05-04)
v0.3 dev ✅ Live at https://uncharted.eurekamd.cloud/ — Caddy + dist/ + LE TLS, mat-view perf pass shipped 2026-05-04
         ✅ mv_sector_grade_matrix (13.7s → 91ms = 154×)
         ✅ mv_national_treemap (1.15s → 88ms = 13×)
         ✅ MapMode code-split (Locust users skip 467 KB gz on first paint)
         ✅ Caddy gzip + immutable-cache headers on /assets/*
v0.3 P4  📋 Production deploy (analyzer.denue.net) — separate from this dev URL
```

## What's in the live DB

### Establecimientos (the spine)

`establecimientos` — 6,097,681 rows × wide schema. Backfilled join keys:

- `entidad` (CVE_ENT, 2-char) — 100% populated
- `area_geo` (CVE_MUN_5, 5-char ENT||MUN) — backfilled from CLEE chars 1-5
- `sector_actividad_id` / `subsector_actividad_id` / `rama_actividad_id` /
  `subrama_actividad_id` / `clase_actividad_id` (SCIAN 2/3/4/5/6-digit) —
  backfilled from CLEE chars 6-11
- `ageb` (13-char CVEGEO ENT+MUN+LOC+AGEB) — backfilled via PostGIS spatial
  join against `ageb_polygons`. 99.99975% coverage (15 unmatched are bad
  lat/lon).

Indexes: btree on entidad, area_geo, clase_actividad_id, sector_actividad_id;
GIST on geom; GIN on nombre.

### Polygon tables (Tier 2.5, from MGN 2020)

| Table           | Rows   | Use                                            |
| --------------- | ------ | ---------------------------------------------- |
| `ent_polygons`  | 32     | entidad outlines (map base)                    |
| `mun_polygons`  | 2,469  | municipio outlines (map base + spatial joins)  |
| `loc_polygons`  | 50,308 | localidades urbanas amanzanadas                |
| `ageb_polygons` | 81,451 | AGEB urbana polygons (cvegeo, cve\_\*, ambito) |

All SRID 4326, GIST-indexed.

### v0.2.2 Health infrastructure (CLUES, this commit batch)

| View / Table                      | Rows   | What it gives you                                                                                                                                                                               |
| --------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `clues_raw` (table, 68 TEXT cols) | 63,708 | Verbatim DGIS CLUES catálogo (ENERO 2026 cut, 8 instituciones, 41k EN OPERACION + 22k FUERA + ~60 construcción)                                                                                 |
| `clues` (mat. view, 20 cast cols) | 41,381 | Filtered to EN OPERACION. cve_mun + cve_loc + nivel_atencion::int + lat/lon + geom POINT(4326). 39,946 (96.5%) geocoded. GIST on geom + btree on cve_mun, cve_loc, institucion, nivel_atencion. |

**Join keys**:

- `cve_mun` (5-char) — joins to censo_municipios, coneval_pobreza_municipal, coneval_irs_municipal, establecimientos.area_geo
- `cve_loc` (9-char ENT+MUN+LOC) — joins to loc_polygons.cvegeo
- `geom` (PostGIS POINT 4326) — `ST_DWithin(c.geom::geography, e.geom::geography, <metros>)` for proximity to establecimientos

**Smoke verified 2026-05-04**:

- Iztapalapa (CDMX, ~1.8M habitantes, 43.9% pobreza): 512 unidades CLUES (347 primer nivel + 165 segundo/tercer nivel)
- Puebla city (cve_mun=21114): 2,652 farmacias DENUE (4659% codes), 752 CLUES units within 2km
- Top 10 "desierto de salud" (CLUES por 100k <12) are exactly the expected peripheral metro suburbs: Kanasín (Mérida), Chimalhuacán (CDMX), García/Juárez/Gral. Zuazua (NL/Monterrey), Tlajomulco/Tonalá (GDL), Ecatepec (EdoMex)

### v0.2.1 Census + Welfare

| View / Table                                          | Rows    | What it gives you                                                                                              |
| ----------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------- |
| `censo_iter` (table, 286 TEXT cols)                   | 195,662 | National + 32 state + 2,469 municipal + 193k locality aggregates from INEGI Censo 2020                         |
| `censo_municipios` (view, 14 cast cols)               | 2,469   | pobtot, pobfem, p_60ymas, pea, pocupada, graproes, tvivhab, vph_inter, etc.                                    |
| `coneval_pobreza_municipal_raw` (table, 37 TEXT cols) | 2,469   | Verbatim CONEVAL pobreza CSV                                                                                   |
| `coneval_pobreza_municipal` (view, 18 cast cols)      | 2,469   | % pobreza / extrema / moderada, vulnerabilidad, 6 carencias sociales, línea de pobreza por ingreso             |
| `coneval_irs_municipal_raw` (table, 19 TEXT cols)     | 2,469   | Verbatim CONEVAL IRS XLSX (Municipios sheet)                                                                   |
| `coneval_irs_municipal` (view, 17 cast cols)          | 2,469   | analfabetismo, asistencia escolar, derechohabiencia, calidad vivienda × 7, IRS índice + grado + lugar nacional |

**Join key**: `cve_mun` (5-char zero-padded ENT||MUN) — same value across all 4 views.

**Sample 4-source join**: see `scripts/load-coneval.ts` header for query.

### Phase 5 + P1 HTTP API (separate scope)

`scripts/serve.ts` exposes 12 endpoints under X-Api-Key auth (8 originales

- 4 nuevos `/analytics/*` shipped en v0.3 P2). Documented in main README.

### v0.3 P2 — Locust mode (analyzer frontend, this commit batch)

5 ECharts in a 2-row + 3-column dashboard, cross-filtered through Zustand:

| Chart                | Source endpoint(s)                | Reads as                                                                                       |
| -------------------- | --------------------------------- | ---------------------------------------------------------------------------------------------- |
| Mosaico nacional     | `/analytics/national-treemap`     | 32 entidades sized by establecimientos, colored by modal IRS grade. Click → set entidad filter |
| Sector × IRS heatmap | `/analytics/sector-grade-matrix`  | 19 SCIAN sectors × 5 grados de rezago. Spots structural sector-welfare patterns                |
| Top 10 sectores      | `/analytics/top-sectors?entidad=` | Bypasses unrefreshed mv_sector_summary by aggregating directly via indexed sector_actividad_id |
| Densidad vs Pobreza  | `/analytics/municipios?entidad=`  | Scatter, X=estab/1k hab, Y=pobreza_pct, size=pob, color=IRS grade                              |
| Salud cobertura      | `/analytics/municipios?entidad=`  | CLUES públicas vs farmacias DENUE per 100k hab, top 10 municipios por población                |

Stack additions: `echarts@6.0.0` + `echarts-for-react@3.0.6` (matches the
frozen-stack plan, brings analyzer to ~1.5 MB JS / 486 KB gzipped).

## What blocks v0.2.x from full closure

**v0.2.1 — AGEB-level disaggregates**:

- INEGI `RESAGEBURB` (Censo 2020 by AGEB and manzana) — per-state CSVs
  gated behind `inegi.org.mx/app/scitel/Default?ev=10` SCITEL portal.
  UA spoofing alone doesn't crack it. **Operator-assist needed.**
- CONEVAL "Grado de Rezago Social en AGEB urbanas 2020" — same pattern,
  separate gated portal at coneval.org.mx.

**v0.2.2 — CE 2024 (Censo Económico)**:

- Datos abiertos URL pattern (`/contenidos/programas/ce/2024/datosabiertos/...`)
  returns the 2,263-byte UA-gating decoy across 4 candidate paths probed
  2026-05-04. SAIC portal at `/app/saic/` is alive but the actual CSV/ZIP
  downloads are routed through the JS-rendered SPA on
  `censoseconomicos2024.mx`. **Operator-assist needed**: open the page in a
  browser, navigate to "Datos abiertos" tab, copy the per-state ZIP URLs.

**v0.2.2 — SESNSP (Incidencia Delictiva)**:

- Both gob.mx slugs in the Fase 2 doc (`acciones-y-programas/datos-abiertos-de-incidencia-delictiva`
  and `acciones-y-programas/incidencia-delictiva-del-fuero-comun-nueva-metodologia`)
  return 404 in 2026-05-04. The SESNSP IDEFC dataset has likely moved.
  **Operator-assist needed**: paste the current SESNSP datos-abiertos
  landing URL or a direct CSV link.

**Resume protocol when a portal-gated source is unblocked**: operator
hands me file path or signed URL → I extend the existing loader pattern
(see `scripts/load-clues.ts` for the canonical CSV-via-docker-cp shape).

## Loader scripts (all idempotent, all in `scripts/`)

| Script             | Inputs                          | What it does                                                                        |
| ------------------ | ------------------------------- | ----------------------------------------------------------------------------------- |
| `load-censo.ts`    | `--csv=ITER.csv`                | DROP+CREATE `censo_iter`, `\copy` with NULL '\*', recreate `censo_municipios` view  |
| `load-coneval.ts`  | `--pobreza=p.csv --irs=irs.csv` | DROP+CREATE both raw tables, `\copy`, recreate both views                           |
| `load-clues.ts`    | `--csv=clues.csv`               | DROP+CREATE `clues_raw`, `\copy`, recreate `clues` mat. view + GIST + btree indexes |
| `backfill-ageb.ts` | (no flags)                      | Spatial-join UPDATE on establecimientos via ageb_polygons                           |
| `pipeline.ts`      | `--all` / `--estados=09`        | Full DENUE national or per-state extraction (prints reminder to run backfill-ageb)  |

All hardened with: csvPath leading-`-` rejection, dbContainer regex, `cp --` separator, try/finally cleanup, quoted SQL identifiers, NULLIF guards, mocked orchestration tests.

## Tests

**308 passing across 32 files** (238 backend src + 38 backend scripts + 32
web). Each loader has unit tests for SQL composition + orchestration
mocked tests. Live DB never touched in tests. Trajectory:

- Phase 5 + P1: 28
- v0.2.1 loaders: +35 (load-censo + load-coneval)
- v0.2.2 loaders: +12 (load-clues)
- v0.3 P2 + P3 + perf pass + audits: +56 (analytics.test.ts grew 0 → 41,
  web Zod + map + useUrlSync added 32, App.tsx code-split untested
  pending RTL setup)

## Mat-view refresh cadence

The `/analytics/sector-grade-matrix` and `/analytics/national-treemap`
endpoints read from materialized views built by `scripts/perf-matviews.sql`.
After a DENUE pipeline reload (`npx tsx scripts/pipeline.ts --all`),
counts drift from the live `establecimientos` table until the views
are rebuilt. Re-run the SQL file to refresh:

```
docker exec -i supabase-db psql -U postgres -d postgres < scripts/perf-matviews.sql
```

The DDL is idempotent (DROP+CREATE = full refresh), takes ~15s. The
pipeline.ts completion message reminds the operator. Backend handlers
fall back to live SQL on `relation does not exist`, so analyzer keeps
working on a fresh DB that hasn't run the migration — just 100-150×
slower.

## What's queued for v0.2.2 (still)

| Dataset                           | Join key                      | Provider | Status                                                                                                       |
| --------------------------------- | ----------------------------- | -------- | ------------------------------------------------------------------------------------------------------------ |
| **CE 2024** (Censo Económico)     | cve_mun + sector_actividad_id | INEGI    | ⏳ Operator-assist needed: gated SAIC/SPA portal, datos-abiertos ZIP URL not externally derivable            |
| **SESNSP** (incidencia delictiva) | cve_mun                       | SESNSP   | ⏳ Operator-assist needed: gob.mx slugs in fase-2 doc 404 (2026-05-04), current dataset URL must be supplied |

CLUES already shipped — see "v0.2.2 Health infrastructure" section above.

## What's queued for v0.2.3

| Dataset                                     | Join level                | Provider |
| ------------------------------------------- | ------------------------- | -------- |
| **Datatur** (Compendio Estadístico Turismo) | state + select municipios | SECTUR   |
| **SINAIS** (mortalidad / morbilidad)        | cve_mun + ICD codes       | DGIS     |
| **ENOE** (empleo y ocupación)               | state + sector            | INEGI    |
| **ENIGH** (ingresos hogares)                | state + AGEB select       | INEGI    |

## On resume after `/compact`

1. `cat docs/v0.2-status.md` (this file) to re-orient.
2. `git log --oneline -10` for what shipped.
3. `npx tsc --noEmit` (root + `web/`) to verify typecheck. Don't run bare
   `vitest run` — the scope-guard hook blocks it; pre-commit on the latest
   commit ran the full suite green.
4. Live-smoke the analyzer: `cd web && npm run dev` → http://localhost:5173.
   Backend served by `npx tsx --env-file=.env scripts/serve.ts`.
5. Pick one of the next moves:
   - **v0.3 P4 — Deploy**: rsync `web/dist/` to `/var/www/denue-analyzer`,
     Caddy block at `analyzer.denue.net`, LE TLS. ~3-4 hours. Plan §P4.
   - **v0.2.2 closure**: ask operator for CE 2024 per-state ZIP URLs
     (gated SAIC/SPA portal) and current SESNSP datos-abiertos URL
     (gob.mx slug 404'd 2026-05-04).
   - **v0.3.1 audit follow-ups**: from Map mode audit — W3 (stable-callback
     contract for MapShell props, document or wrap in latest-callback ref),
     R5 (code-split deck.gl + maplibre into a Map-mode async chunk so
     Locust users never pay the deck.gl cost — saves ~600 KB on first paint).
     From Locust audit — W2 (FilterPanel empty-string vs null), W3 (dead
     `?? 1` fallbacks), R1 (surface execFileSync stderr in 502 message).

Don't re-deliberate the sealed v0.2.1 + v0.2.2-CLUES + v0.3-P2-Locust

- v0.3-P3-Map decisions — every loader pattern is hardened + tested +
  audited, every endpoint is X-Api-Key gated and Zod-validated client-side,
  the views are live, and both Locust + Map modes are functionally complete.

## Disk + DB state at compact

- VPS disk: 12 GB free / 89% used (CLUES adds ~70 MB total).
- PG `establecimientos`: 15 GB (the bulk).
- Polygon tables: ~404 MB combined.
- v0.2.1 tables + views: <100 MB combined (Censo+CONEVAL are small).
- v0.2.2 tables + views: ~70 MB (CLUES raw + materialized view + indexes).
- v0.3 P2: no DB additions (analytics endpoints aggregate on the fly).
- Web bundle: ~1.5 MB JS / 486 KB gzipped (`web/dist/`).
- Source files in `/opt/data/`: deleted after load — clean.
